#include "AppUtil.hpp"
#include "App.hpp"

#include "Util/Logger.hpp"

void App::ValidTask() {
    bool StrawberryIsEaten;
    bool ChessIsOpened;
    LOG_DEBUG("Validating the task {}", static_cast<int>(m_Phase));
    switch (m_Phase) {
        case Phase::Phase00:

            break;

        case Phase::Phase01:

            break;

        case Phase::Phase03:

            break;

        case Phase::Phase04:

            break;

        case Phase::Phase05:

            break;

        case Phase::Phase06:

            break;
    }
}
